# Research

One file per research topic. Copy `_template.md`, rename it to
`a-few-words.md`, and fill it in. The name becomes the address:
`guiding-vector-fields.md` → `/research/guiding-vector-fields`.

Topics are ordered by the `order` field — lower numbers first.

Pictures go in `images/`.
