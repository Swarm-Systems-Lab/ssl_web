Standalone blocks of text used by a fixed page.

- `home.md` — the introduction on the front page.

These are not templates: each file is wired to a specific place on the site, so
add a new one only together with a developer.
